apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "imh.fullname" . }}-test-connection"
  namespace: {{ .Values.namespace | default .Release.Namespace }}
  labels:
    {{- include "imh.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "imh.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
