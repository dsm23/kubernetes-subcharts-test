apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "svelte-kit.fullname" . }}-test-connection"
  namespace: {{ .Values.namespace | default .Release.Namespace }}
  labels:
    {{- include "svelte-kit.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "svelte-kit.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
