apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "next.fullname" . }}-test-connection"
  namespace: {{ .Values.namespace | default .Release.Namespace }}
  labels:
    {{- include "next.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "next.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
